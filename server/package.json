{
  "name": "turma-mais-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API do Turma+ (Express + SQLite, JWT, SSE, Web Push)",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "seed": "node src/db/seed.js",
    "reset": "rm -f data/turma.db && node src/db/seed.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "web-push": "^3.6.7",
    "zod": "^3.24.1"
  }
}
